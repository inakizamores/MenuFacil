// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Dummy model just to satisfy Prisma generation
// We're using Supabase for database access in this project
model DummyModel {
  id        String   @id @default(uuid())
  createdAt DateTime @default(now())
}

// Note: We are using Supabase for database access in this project
// This schema file is here to satisfy the build process
// We don't actually use Prisma Client in the application 